import { getNewBoard, randomNumber } from './random/random';
import {
  shuffle,
  moveCell,
  canMoveСell,
  isSolved,
  updateSolutionPath,
  moveInDirection,
} from './common/common';

export {
  moveInDirection,
  getNewBoard,
  randomNumber,
  updateSolutionPath,
  shuffle,
  moveCell,
  canMoveСell,
  isSolved,
};
